---
import "../styles/global.css";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";

interface Props {
  title?: string;
  description?: string;
  transparentHeader?: boolean;
}

const {
  title = "Scott's Ballroom | Premier Event Venue · Downtown San Jose",
  description = "7,163 sq ft event venue at the San Jose Rotary Summit Center. Weddings, corporate galas, and social events above the skyline for 50 to 790+.",
  transparentHeader = false,
} = Astro.props;

const base = import.meta.env.BASE_URL.replace(/\/$/, "");
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    {(Astro.site?.hostname ?? "").endsWith("github.io") && <meta name="robots" content="noindex, nofollow" />}
    <meta name="description" content={description} />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href={`${base}/favicon.svg`} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <title>{title}</title>
    {/* Google Analytics 4 — production only; skipped on github.io previews */}
    {!(Astro.site?.hostname ?? "").endsWith("github.io") && (
      <>
        <script is:inline async src="https://www.googletagmanager.com/gtag/js?id=G-BSB2E8YKBX"></script>
        <script is:inline>
          window.dataLayer = window.dataLayer || [];
          function gtag(){dataLayer.push(arguments);}
          gtag('js', new Date());
          gtag('config', 'G-BSB2E8YKBX');
        </script>
      </>
    )}
  </head>
  <body class="min-h-screen flex flex-col">
    <Header transparent={transparentHeader} />
    <main class="flex-1">
      <slot />
    </main>
    <Footer />
  </body>
</html>
